{
  programs.nvf.settings.vim = {
    theme = {
      enable = true;
      name = "catppuccin";
      style = "mocha";
    };

    visuals = {
      nvim-web-devicons.enable = true;
      rainbow-delimiters.enable = true;
    };

    highlight = {
      # SignColumn.bg =  TODO: Need to do something about this at some point
    };
  };
}
